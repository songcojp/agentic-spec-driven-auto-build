interface:
  display_name: "Feature Implementation"
  short_description: "Implement bounded Feature Spec work. Use when a scheduled Fe"
  default_prompt: "Use $implement-feature to handle the relevant SpecDrive workflow."
policy:
  allow_implicit_invocation: true
