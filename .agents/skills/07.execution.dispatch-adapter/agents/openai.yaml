interface:
  display_name: "Feat Implement Skill"
  short_description: "Implement Feature Specs with review gates"
  default_prompt: "Use $07.execution.dispatch-adapter to execute the scheduled Feature Spec with default subagent delegation, scoped review gates, verification, task status sync, git delivery evidence, and a compact evidence result contract."
