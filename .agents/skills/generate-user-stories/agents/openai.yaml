interface:
  display_name: "User Story Generation"
  short_description: "Generate detailed user stories and requirements from product prose"
  default_prompt: "Use $generate-user-stories to handle the relevant SpecDrive workflow."
policy:
  allow_implicit_invocation: true
