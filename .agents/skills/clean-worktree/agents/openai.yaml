interface:
  display_name: "Worktree Clean"
  short_description: "Clean Feature worktrees after delivery"
  default_prompt: "Use $clean-worktree to clean a delivered Feature worktree."
policy:
  allow_implicit_invocation: true
