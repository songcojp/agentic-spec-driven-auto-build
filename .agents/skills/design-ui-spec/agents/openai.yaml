interface:
  display_name: "UI System Design"
  short_description: "Design UI systems, flows, interaction matrices, and WYSIWYG HTML prototypes."
  default_prompt: "Use $design-ui-spec to handle the relevant SpecDrive workflow."
policy:
  allow_implicit_invocation: true
