interface:
  display_name: "Product Intent Refinement"
  short_description: "Create or refine product intent artifacts. Use for PRD gener"
  default_prompt: "Use $refine-product-intent to handle the relevant SpecDrive workflow."
policy:
  allow_implicit_invocation: true
