interface:
  display_name: "Worktree Setup"
  short_description: "Set up isolated Feature worktrees"
  default_prompt: "Use $setup-worktree to prepare an isolated Feature worktree."
policy:
  allow_implicit_invocation: true
